# photobox-frontend

Photobox web UI. API URLs come from `.env.development.local`.

## Local dev

```bash
# Frontend
make dev
```

Open <http://localhost:5173/login> — dev credentials `dev` / `dev`.

Messenger mini-app locally: open `http://localhost:5173/?mock=messenger` (mock initData; same build as production).
